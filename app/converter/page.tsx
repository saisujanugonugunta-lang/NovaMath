'use client'; import {useState} from 'react'; const units=['Length','Area','Volume','Mass','Time','Speed','Temperature','Pressure','Energy','Power','Angle','Data storage']; export default function Page(){const [v,setV]=useState('1'); const [from,setFrom]=useState('m'); const [to,setTo]=useState('cm'); return <main className="page"><div className="container"><div className="pagehead"><h1>Unit Converter</h1><p>Quick scientific and mathematical unit conversions.</p></div><div className="grid">{units.map(x=><div className="card" key={x}><h3>{x}</h3><p>Converter workspace</p></div>)}</div><div className="panel" style={{maxWidth:760,margin:'24px auto 0'}}><div className="field"><label>Value</label><input value={v} onChange={e=>setV(e.target.value)}/></div><div style={{display:'grid',gridTemplateColumns:'1fr 1fr',gap:12}}><div className="field"><label>From</label><input value={from} onChange={e=>setFrom(e.target.value)}/></div><div className="field"><label>To</label><input value={to} onChange={e=>setTo(e.target.value)}/></div></div><div className="result"><strong>Conversion ready</strong><p>Connect a unit-specific conversion rule for {from} → {to}.</p></div></div></div></main>}
