'use client';
import {useState} from 'react';
import {evaluate} from 'mathjs';
export default function HomeSolver(){const [q,setQ]=useState('');const [r,setR]=useState('');const solve=()=>{if(!q.trim()){setR('');return}try{const normalized=q.replace(/×/g,'*').replace(/÷/g,'/').replace(/√/g,'sqrt');const value=evaluate(normalized);setR(String(value))}catch{setR('I can calculate supported expressions, but this problem needs a specialized solver. Try an expression such as 2+2, sqrt(144), sin(pi/4), or 5^3.')}};return <div className="solver"><div className="solverrow"><input value={q} onChange={e=>setQ(e.target.value)} onKeyDown={e=>e.key==='Enter'&&solve()} placeholder="Enter a mathematical problem..."/><button className="primary" onClick={solve}>Solve</button><button className="secondary" onClick={()=>{setQ('');setR('')}}>Clear</button></div>{r&&<div className="result"><div style={{color:'var(--muted)',fontSize:13}}>Result</div><strong>{r}</strong><p>Calculated safely using the NovaMath expression engine.</p></div>}</div>}
